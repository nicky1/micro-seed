#目前健康检查默认是用的8080端口，这里严禁修改端口号。
server:
  port: 8080

spring:
  application:
    name: waffle

#eureka:
#  instance:
#    leaseRenewalIntervalInSeconds: 1
#    instanceId: ${spring.application.name}:${spring.cloud.client.ipAddress}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
#    preferIpAddress: true
#  client:
#    registryFetchIntervalSeconds: 1
#    serviceUrl:
#      #defaultZone:  http://mse.zhonganonline.com:8761/eureka/
#      defaultZone: http://11516-iyunbao-ifuli-icare-api.test.za.net/eureka

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  refresh:
    enabled: true

health:
  config:
    enabled: false
  sleuth:
    log:
      json:
        enabled: false
      slf4j:
        enabled: false

